{
  "name": "restolink-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Standalone marketing page for RestoLink. Builds to plain static files with no runtime dependency on the app or the API.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "tailwindcss": "^4.1.18",
    "typescript": "~5.9.3",
    "vite": "^7.1.12"
  }
}
